package org.example;

//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main {
    public static void main(String[] args) {
/*
import org.junit.jupiter.api.Test;
import java.io.File;
import static org.junit.jupiter.api.Assertions.assertEquals;

public class FilePathTest {

    @Test
    public void testFilePathHandling_BadPractice() {
        String directory = "src";
        String filename = "config.xml";

        // CỐ TÌNH LÀM SAI: Hardcode dấu gạch chéo của Linux/macOS
        String hardcodedPath = directory + "/" + filename;

        // Hàm new File().getPath() sẽ trả về đường dẫn chuẩn theo HĐH đang chạy
        String systemPath = new File(directory, filename).getPath();

        // Kiểm tra xem đường dẫn tự ghép có khớp với đường dẫn chuẩn của hệ thống không
        assertEquals(systemPath, hardcodedPath,
            "Lỗi: Đường dẫn không tương thích với hệ điều hành hiện tại!");
    }
}
 */
    }
}